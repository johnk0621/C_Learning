// NOTE: Automatic antivirus scanning (Norton) for this entire C folder has been turned off!!!
// Don't tamper with some dumb shit here!

# include <stdio.h> 

/*
Preprocessor Directive
    --> tells compiler to include standard input/output library
    --> req. to display text to screen
*/

int main() { // REQUIRED --> without this you can't run the program --> ERROR
    
    printf("Hello, World!\n"); // end all statements with ; (like Java)
    printf("I like pizza."); // without newline in first printf, everything prints to one line

    return 0; // good practice (for backwards compatibility) --> if return isn't 0, typically an error
}

// This is my first program :)

/*
This is a multi-line comment!
Isn't part of the code, but tells other developers a lot!
*/