# include <stdio.h>
# include <math.h> // new preprocessor directive!

int main() {

    // int x = 9;
    // float x = 3.85;
    int x = -3;

    // x = sqrt(x); // square root
    // x = pow(x, 2); // power function: base, given power
    // x = round(x); // round
    // x = ceil(x); // ceiling function: always rounds up
    // x = floor(x); // floor function: always rounds down
    // x = abs(x); // absolute value function
    // x = log(x); // natural log function
    // x = sin(x); in radians
    // x = cos(x); in radians
    // x = tan(x); in radians

    printf("%d", x);

    return 0;
}
